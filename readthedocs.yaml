version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre_build: pip install .[docs]  # install your package + docs extras

python:
  install:
    method: pip
    path: .  # install from current directory

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false